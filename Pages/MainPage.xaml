<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SilvaData.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:SilvaData.Controls"
    xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:d="http://schemas.microsoft.com/dotnet/2021/maui/design"
    xmlns:pageModels="clr-namespace:SilvaData.PageModels"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TabView;assembly=Syncfusion.Maui.Toolkit"
    xmlns:localization="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:views="clr-namespace:SilvaData.Pages"
    x:Name="mainPage"
    x:DataType="pageModels:MainPageModel"
    Background="{StaticResource QuaseBranco}"
    Shell.NavBarIsVisible="false">

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{StaticResource Primary}" StatusBarStyle="LightContent" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <Style x:Key="BaseTabStyle" TargetType="sf:SfTabItem">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="FontFamily" Value="FontAwesomeSolid" />
        </Style>

        <Style
            x:Key="MercadosTabStyle"
            BasedOn="{StaticResource BaseTabStyle}"
            TargetType="sf:SfTabItem" />

        <Style
            x:Key="GalpoesTabStyle"
            BasedOn="{StaticResource BaseTabStyle}"
            TargetType="sf:SfTabItem" />

        <Style
            x:Key="LotesTabStyle"
            BasedOn="{StaticResource BaseTabStyle}"
            TargetType="sf:SfTabItem" />

        <Style
            x:Key="SyncTabStyle"
            BasedOn="{StaticResource BaseTabStyle}"
            TargetType="sf:SfTabItem" />

        <Style
            x:Key="ContaTabStyle"
            BasedOn="{StaticResource BaseTabStyle}"
            TargetType="sf:SfTabItem" />
    </ContentPage.Resources>

    <Grid Background="{StaticResource QuaseBranco}" RowDefinitions="*">


        <sf:SfTabView
            x:Name="mainTabView"
            Grid.Row="0"
            HeaderDisplayMode="Image"
            IndicatorBackground="{StaticResource PrimaryBrush}"
            IndicatorPlacement="Bottom"
            IndicatorWidthMode="Stretch"
            IsVisible="{Binding IsBusy, Converter={StaticResource InvertBoolean}}"
            SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
            TabBarBackground="{StaticResource QuaseBranco}"
            TabBarHeight="70"
            TabBarPlacement="Bottom"
            VerticalOptions="Fill">

            <sf:SfTabItem Style="{StaticResource MercadosTabStyle}">
                <sf:SfTabItem.HeaderContent>
                    <Grid
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        HeightRequest="28"
                        WidthRequest="28">
                        <!-- Unselected Icon -->
                        <Image
                            Aspect="AspectFit"
                            HeightRequest="26"
                            WidthRequest="26"
                            Source="iconmercado.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Gray300}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.MercadosSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                        <!-- Selected Icon -->
                        <Image
                            Aspect="AspectFit"
                            HeightRequest="26"
                            WidthRequest="26"
                            IsVisible="False"
                            Source="iconmercado.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Primary}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.MercadosSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="True" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                    </Grid>
                </sf:SfTabItem.HeaderContent>
                <controls:MercadosDashboardView />
            </sf:SfTabItem>

            <sf:SfTabItem Style="{StaticResource GalpoesTabStyle}">
                <sf:SfTabItem.HeaderContent>
                    <Grid
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        HeightRequest="34"
                        WidthRequest="34">
                        <Image
                            Aspect="AspectFit"
                            Margin="0,5,0,0"
                            HeightRequest="37"
                            WidthRequest="37"
                            Source="icongalpao.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Gray300}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.GalpoesSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                        <Image
                            Aspect="AspectFit"
                            Margin="0,5,0,0"
                            HeightRequest="37"
                            WidthRequest="37"
                            IsVisible="False"
                            Source="icongalpao.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Primary}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.GalpoesSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="True" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                    </Grid>
                </sf:SfTabItem.HeaderContent>
                <controls:GalpoesView />
            </sf:SfTabItem>

            <sf:SfTabItem Style="{StaticResource LotesTabStyle}">
                <sf:SfTabItem.HeaderContent>
                    <Grid
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        HeightRequest="34"
                        WidthRequest="34">
                        <Image
                            Aspect="AspectFit"
                            HeightRequest="34"
                            WidthRequest="34"
                            Source="iconlotes.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Gray300}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.LotesSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                        <Image
                            Aspect="AspectFit"
                            HeightRequest="34"
                            WidthRequest="34"
                            IsVisible="False"
                            Source="iconlotes.png">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{StaticResource Primary}" />
                            </Image.Behaviors>
                            <Image.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.LotesSelected, Source={x:Reference mainPage}}"
                                    TargetType="Image"
                                    Value="True">
                                    <Setter Property="IsVisible" Value="True" />
                                </DataTrigger>
                            </Image.Triggers>
                        </Image>
                    </Grid>
                </sf:SfTabItem.HeaderContent>
                <controls:LoteView />
            </sf:SfTabItem>

            <sf:SfTabItem Style="{StaticResource SyncTabStyle}">
                <sf:SfTabItem.HeaderContent>
                    <Grid HeightRequest="35" WidthRequest="35">
                        <Label
                            FontFamily="FontAwesomeSolid"
                            FontSize="24"
                            HorizontalOptions="Center"
                            Text="&#xf021;"
                            TextColor="{StaticResource Gray300}"
                            VerticalOptions="Center">
                            <Label.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.SyncSelected, Source={x:Reference mainPage}}"
                                    TargetType="Label"
                                    Value="True">
                                    <Setter Property="TextColor" Value="{StaticResource Primary}" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                        <Border
                            Margin="0,-8,-8,0"
                            Padding="0"
                            Background="#E53935"
                            HeightRequest="18"
                            WidthRequest="18"
                            HorizontalOptions="End"
                            VerticalOptions="Start"
                            StrokeThickness="0"
                            IsVisible="{Binding SyncPendingCount, Converter={StaticResource IsNotNullOrEmptyConverter}}">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="9" />
                            </Border.StrokeShape>
                            <Label
                                Padding="0"
                                FontSize="10"
                                FontAttributes="Bold"
                                HorizontalTextAlignment="Center"
                                VerticalTextAlignment="Center"
                                TextColor="White"
                                Text="{Binding SyncPendingCount}" />
                        </Border>
                    </Grid>
                </sf:SfTabItem.HeaderContent>
                <controls:SincronizacaoPendentesView />
            </sf:SfTabItem>

            <sf:SfTabItem Style="{StaticResource ContaTabStyle}">
                <sf:SfTabItem.HeaderContent>
                    <Grid HeightRequest="34" WidthRequest="34">
                        <Label
                            FontFamily="FontAwesomeSolid"
                            FontSize="24"
                            HorizontalOptions="Center"
                            Text="&#xf007;"
                            TextColor="{StaticResource Gray300}"
                            VerticalOptions="Center">
                            <Label.Triggers>
                                <DataTrigger
                                    Binding="{Binding BindingContext.ContaSelected, Source={x:Reference mainPage}}"
                                    TargetType="Label"
                                    Value="True">
                                    <Setter Property="TextColor" Value="{StaticResource Primary}" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </Grid>
                </sf:SfTabItem.HeaderContent>
                <controls:MinhaConta />
            </sf:SfTabItem>

        </sf:SfTabView>

        <Grid
            Grid.Row="0"
            IsVisible="{Binding IsBusy}">

            <Grid.Background>
                <SolidColorBrush Color="White" />
            </Grid.Background>

            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="40">

                <!-- Large Centered Logo -->
                <Image
                    Source="silvadatalogo"
                    WidthRequest="250"
                    HeightRequest="250"
                    Aspect="AspectFit"
                    HorizontalOptions="Center">
                    <Image.Shadow>
                        <Shadow Brush="Black" Opacity="0.3" Radius="20" Offset="0,10" />
                    </Image.Shadow>
                </Image>

                <!-- Centered Loading Progress -->
                <VerticalStackLayout Spacing="15" HorizontalOptions="Center">
                    <ActivityIndicator
                        IsRunning="True"
                        Color="{StaticResource Primary}"
                        HorizontalOptions="Center"
                        HeightRequest="40"
                        WidthRequest="40" />
                    <Label
                        Text="{Binding LoadingText}"
                        TextColor="{StaticResource Primary}"
                        FontSize="20"
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        HorizontalOptions="Center" />
                </VerticalStackLayout>
            </VerticalStackLayout>
        </Grid>

    </Grid>
</ContentPage>
